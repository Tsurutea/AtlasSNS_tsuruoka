spring.application.name=AtlasSNS_tsuruoka

<<<<<<< HEAD
# DB接続情報（MySQL用）
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# DB接続URL（MySQLのPort番号）
spring.datasource.url=jdbc:mysql://localhost:3306/atlas_sns

# DBログイン情報
spring.datasource.username=root
spring.datasource.password=root

# Hibernate のSQLログ出力
spring.jpa.show-sql=true

# schema.sql と data.sql を自動実行
spring.sql.init.mode=always

# テーブル自動生成設定（DDL生成しない）
spring.jpa.hibernate.ddl-auto=none
=======
# --- DB接続設定 ---
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/atlas_sns?useSSL=false&characterEncoding=UTF-8&serverTimezone=Asia/Tokyo
spring.datasource.username=root
spring.datasource.password=root

# --- JPA設定 ---
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=update

# --- schema.sql / data.sql 実行 ---
spring.sql.init.mode=never

# --- ファイルアップロード設定 ---
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# --- ユーザーアイコン保存先（相対パスに変更） ---
#   ./user-icons → プロジェクト実行ディレクトリ直下に保存される
app.user-icon-dir=./user-icons

# --- 静的リソースとして配信したい場合（オプション） ---
spring.web.resources.static-locations=classpath:/static/,file:./user-icons/
>>>>>>> 2f38405 (提出)
